require 'test_helper'

class ExpensesControllerTest < ActionDispatch::IntegrationTest
  # test "the truth" do
  #   assert true
  # end

  # def current_user
  #   return unless session[:user_id]
  #   @current_user ||= User.find(session[:user_id])
  # end

  # a get http request was sent to the router, which pulls up the controller#index, and that response should be 200.
  # test "should get index" do
  #
  #   @current_user ||= User.new()
  #   get '/expenses/index'
  #   assert_response :success
  # end

end
